schema_version: 2
type: train
datasets:
  - id: replace-with-dataset-id
backend:
  name: ai-toolkit
  config:
    command:
      cwd: /workspace
      argv: [python, -c, "print('kura docker smoke')"]
      env: {}
