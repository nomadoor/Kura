# Issue labels

Reuse the repository's existing GitHub labels when an explicitly approved
issue is created. The current labels are the standard GitHub set, including
`bug`, `documentation`, `enhancement`, and `question`.

There is no agent-readiness label. Do not create one without explicit user
approval. Label selection never authorizes implementation or publication.
